# DEPS.yml for MNIST BNN Project

tb_pipe:
  deps:
    - ../src/pipe.sv
    - tb_pipe.sv
  top: tb_pipe

tb_reset_pipe:
  deps:
    - ../src/reset_pipe.sv
    - tb_reset_pipe.sv
  top: tb_reset_pipe

tb_registers:
  deps:
    - ../src/pipe.sv
    - ../src/registers.sv
    - tb_registers.sv
  top: tb_registers